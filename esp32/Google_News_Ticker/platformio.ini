[env:esp32-s3]
platform = espressif32
board = esp32-s3-devkitc-1
framework = arduino

; Critical: tells the compiler PSRAM exists
build_flags = 
    -DBOARD_HAS_PSRAM
    -DCONFIG_SPIRAM_USE=1

; Tells the linker to place static data intelligently
board_build.arduino.memory_type = qio_opi